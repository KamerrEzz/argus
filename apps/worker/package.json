{
  "name": "@acr/worker",
  "version": "0.1.0",
  "private": true,
  "description": "Queue worker: runs review pipelines, sandboxes checks and publishes results",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -b",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "typecheck": "tsc -b --force"
  },
  "dependencies": {
    "@acr/ai": "*",
    "@acr/config": "*",
    "@acr/database": "*",
    "@acr/github": "*",
    "@acr/pipeline": "*",
    "@acr/queue": "*",
    "@acr/sandbox": "*",
    "@acr/shared": "*",
    "bullmq": "^5.81.5",
    "ioredis": "^5.11.1",
    "zod": "^4.6.5"
  }
}
