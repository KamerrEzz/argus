{
  "name": "@acr/api",
  "version": "0.1.0",
  "private": true,
  "description": "HTTP API: sessions, repositories, reviews, live events and GitHub webhooks",
  "main": "./dist/index.js",
  "scripts": {
    "build": "tsc -b",
    "dev": "tsx watch src/index.ts",
    "start": "node dist/index.js",
    "typecheck": "tsc -b --force"
  },
  "dependencies": {
    "@acr/ai": "*",
    "@acr/config": "*",
    "@acr/database": "*",
    "@acr/github": "*",
    "@acr/pipeline": "*",
    "@acr/queue": "*",
    "@acr/sandbox": "*",
    "@acr/shared": "*",
    "@fastify/cookie": "^11.0.2",
    "@fastify/cors": "^10.1.0",
    "@fastify/rate-limit": "^10.3.0",
    "fastify": "^5.12.5",
    "jose": "^5.10.0",
    "zod": "^4.6.5"
  }
}
